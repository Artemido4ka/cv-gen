import { FormatTranslationPipe } from './format-translation.pipe';

describe('FormatTranslationPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTranslationPipe();
    expect(pipe).toBeTruthy();
  });
});
